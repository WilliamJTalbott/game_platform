button.hand-card type="button" disabled=(!can_select) data-action="click->rummy-turn#toggleSelect" data-rummy-turn-card-param="#{hand_card.card.rank}-#{hand_card.card.suit}"
  .card-container.card-container--large
    .playing-card class=("playing-card--selected" if hand_card.selected)
      = image_tag("dark_cards/#{hand_card.card.rank.downcase}_#{hand_card.card.suit.downcase}.svg")
