---
description: Update AGENTS.md and docs to reflect what this session learned and changed
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git log:*), Read, Edit, Write, Glob, Grep
---

Update AGENTS.md with anything you learned about this project during our session.
Also update any docs that need to reflect what we changed.

Keep it tight — only add what a future agent would genuinely need to know, and
prefer editing or removing stale lines over piling on new ones. Don't restate what
the code, tests, or git history already make obvious. The goal is less bloat, not
more words.
