import { Types } from '@honeybadger-io/core';
export declare class BrowserFeedbackForm {
    private readonly config;
    private readonly logger;
    private readonly scriptUrl;
    constructor(config: Types.BrowserConfig, logger: Types.Logger, scriptUrl: string);
    private appendUserFeedbackTag;
    private isUserFeedbackUrlAlreadyVisible;
    show(lastNoticeId: string, options?: Types.UserFeedbackFormOptions): void;
}
//# sourceMappingURL=feedback-form.d.ts.map